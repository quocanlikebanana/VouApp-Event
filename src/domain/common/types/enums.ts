export enum Metric {
    TOP = "TOP",
    RANK = "RANK",
    SCORE = "SCORE",
}

export enum RewardValueType {
    PROMOTION = "PROMOTION",
    PUZZLE = "PUZZLE",
}
export enum EventStatus {
    PENDING = "PENDING",
    APPROVED = "APPROVED",
    REJECTED = "REJECTED",
    STARTED = "STARTED",
    ENDED = "ENDED"
}
